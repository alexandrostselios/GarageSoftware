@model GarageManagementSoftwarePortal.Models.Followers

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Followers</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="User1" />
            <input type="hidden" asp-for="User2" />
            <div class="form-group">
                <label asp-for="DateOfConnection" class="control-label"></label>
                <input asp-for="DateOfConnection" class="form-control" />
                <span asp-validation-for="DateOfConnection" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a class="af" asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
