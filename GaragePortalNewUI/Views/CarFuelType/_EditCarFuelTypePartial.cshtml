@model GaragePortalNewUI.Models.CarFuelType
@inject LanguageService Resource
@{
    ViewData["Title"] = "partial Car Editted";
}

<div id="editCarFuelTypeItem" name="editCarFuelTypeItem" class="modal" tabindex="-1" role="dialog">
    <div class="modal-dialog draggable" role="document">
        <div class="modal-content">
            <div class="modal-header dragable_touch d-block">
                <h5 class="modal-title">@Resource.GetKey("Edit_Fuel_Type")<h5>
            </div>
            <form asp-action="EditCarFuelTypeItem" enctype="multipart/form-data">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <input type="hidden" asp-for="ID" />
                <input type="hidden" asp-for="GarageID" />
                <div class="modal-body">
                    <div class="form-group">
                        <label asp-for="FuelType" class="control-label">@Resource.GetKey("Fuel_Type")</label>
                        <input asp-for="FuelType" class="form-control" />
                        <span asp-validation-for="FuelType" class="text-danger"></span>
                    </div>
                    <div class="modal-footer">
                        <button type="submit" value="Create" class="btn btn-success">@Resource.GetKey("Edit_Car_Details")</button>
                        <button type="button" class="btn btn-danger" data-dismiss="modal">@Resource.GetKey("Cancel")</button>
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>

<script type="text/javascript">
    $(document).ready(function () {
        $('.draggable').draggable({
            handle: '.modal-header'
        });
    });
</script>